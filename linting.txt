echo "Lint Bash Scripts"
Lint Bash Scripts
find  -iname *.sh | xargs shellcheck disable=SC2010

In ./src/scrape-api/upload_docker.sh line 19:
docker login --username $DOCKER_ID --password $DOCKER_PASSWORD
                                              ^--------------^ SC2086: Double quote to prevent globbing and word splitting.

Did you mean: 
docker login --username $DOCKER_ID --password "$DOCKER_PASSWORD"


In ./bash/path/tf-init.sh line 21:
    tfList=$(ls -l | grep -ie  .tf$ | awk '{print $9}')
             ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-init.sh line 22:
    tfVarList=$(ls -l | grep -ie  .tfvars$ | awk '{print $9}')
                ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-init.sh line 69:
    echo 'terraform init -backend-config="s3_backend.tfvars" \'
                                                             ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-init.sh line 70:
    echo '  -var-file="../../secret.tfvars" \'
                                            ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-plan.sh line 21:
    tfList=$(ls -l | grep -ie  .tf$ | awk '{print $9}')
             ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-plan.sh line 22:
    tfVarList=$(ls -l | grep -ie  .tfvars$ | awk '{print $9}')
                ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-plan.sh line 75:
    echo 'terraform plan -var-file="../../secret.tfvars" \'
                                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-plan.sh line 76:
    echo '  -var-file="../../env.tfvars" \'
                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-plan.sh line 77:
    echo '  -var-file="../subenv.tfvars" \'
                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-plan.sh line 78:
    echo '  -var-file="feature.tfvars" \'
                                       ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-plan.sh line 79:
    echo '  -var "commit_tag=$(git rev-parse --short HEAD)" \'
         ^-- SC2016: Expressions don't expand in single quotes, use double quotes for that.
                                                            ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 21:
    tfList=$(ls -l | grep -ie  .tf$ | awk '{print $9}')
             ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-destroy.sh line 22:
    tfVarList=$(ls -l | grep -ie  .tfvars$ | awk '{print $9}')
                ^-- SC2010: Don't use ls | grep. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.


In ./bash/path/tf-destroy.sh line 75:
    echo 'terraform plan -var-file="../../secret.tfvars" \'
                                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 76:
    echo '  -var-file="../../env.tfvars" \'
                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 77:
    echo '  -var-file="../subenv.tfvars" \'
                                         ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 78:
    echo '  -var-file="feature.tfvars" \'
                                       ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 79:
    echo '  -var "commit_tag=$(git rev-parse --short HEAD)" \'
         ^-- SC2016: Expressions don't expand in single quotes, use double quotes for that.
                                                            ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.


In ./bash/path/tf-destroy.sh line 80:
    echo '  -out plan.out \'
                          ^-- SC1003: Want to escape a single quote? echo 'This is how it'\''s done'.

For more information:
  https://www.shellcheck.net/wiki/SC2010 -- Don't use ls | grep. Use a glob o...
  https://www.shellcheck.net/wiki/SC1003 -- Want to escape a single quote? ec...
  https://www.shellcheck.net/wiki/SC2016 -- Expressions don't expand in singl...
